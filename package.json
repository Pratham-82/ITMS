{
  "name": "apexresolve-root",
  "version": "1.0.0",
  "description": "Root workspace for ApexResolve Complaint Management System",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build --prefix frontend",
    "start": "node -e \"process.env.NODE_ENV='production'; require('./backend/server.js')\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
